<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petrescue.petrescuesystem.mapper.PostMapper">

    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="com.petrescue.petrescuesystem.entity.Post">
        <id column="post_id" property="postId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="view_count" property="viewCount" jdbcType="INTEGER"/>
        <result column="like_count" property="likeCount" jdbcType="INTEGER"/>
        <result column="comment_count" property="commentCount" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 帖子详情结果映射 -->
    <resultMap id="PostDetailResultMap" type="com.petrescue.petrescuesystem.mapper.PostMapper$PostDetail">
        <id column="post_id" property="postId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="view_count" property="viewCount" jdbcType="INTEGER"/>
        <result column="like_count" property="likeCount" jdbcType="INTEGER"/>
        <result column="comment_count" property="commentCount" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="user_post_count" property="userPostCount" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 根据用户ID查询帖子列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM post 
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 分页查询帖子列表 -->
    <select id="selectPostPage" resultMap="BaseResultMap">
        SELECT * FROM post 
        WHERE 1=1
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 查询热门帖子（按浏览量排序） -->
    <select id="selectPopularPosts" resultMap="BaseResultMap">
        SELECT * FROM post 
        ORDER BY view_count DESC, created_at DESC
        LIMIT #{limit}
    </select>

    <!-- 查询最新帖子 -->
    <select id="selectRecentPosts" resultMap="BaseResultMap">
        SELECT * FROM post 
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <!-- 根据关键字搜索帖子 -->
    <select id="searchPosts" resultMap="BaseResultMap">
        SELECT * FROM post 
        WHERE (title LIKE CONCAT('%', #{keyword}, '%') 
               OR content LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY 
            CASE 
                WHEN title LIKE CONCAT(#{keyword}, '%') THEN 1
                WHEN content LIKE CONCAT(#{keyword}, '%') THEN 2
                ELSE 3
            END,
            created_at DESC
    </select>

    <!-- 统计用户的帖子数量 -->
    <select id="countByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM post 
        WHERE user_id = #{userId}
    </select>

    <!-- 增加帖子浏览量 -->
    <update id="incrementViewCount">
        UPDATE post 
        SET view_count = view_count + 1
        WHERE post_id = #{postId}
    </update>

    <!-- 增加帖子点赞数 -->
    <update id="incrementLikeCount">
        UPDATE post 
        SET like_count = like_count + 1
        WHERE post_id = #{postId}
    </update>

    <!-- 减少帖子点赞数 -->
    <update id="decrementLikeCount">
        UPDATE post 
        SET like_count = GREATEST(like_count - 1, 0)
        WHERE post_id = #{postId}
    </update>

    <!-- 增加帖子评论数 -->
    <update id="incrementCommentCount">
        UPDATE post 
        SET comment_count = comment_count + 1
        WHERE post_id = #{postId}
    </update>

    <!-- 减少帖子评论数 -->
    <update id="decrementCommentCount">
        UPDATE post 
        SET comment_count = GREATEST(comment_count - 1, 0)
        WHERE post_id = #{postId}
    </update>

    <!-- 更新帖子状态 -->
    <update id="updateStatus">
        UPDATE post 
        SET status = #{status}, updated_at = NOW()
        WHERE post_id = #{postId}
    </update>

    <!-- 批量更新帖子状态 -->
    <update id="batchUpdateStatus">
        UPDATE post 
        SET status = #{status}, updated_at = NOW()
        WHERE post_id IN 
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </update>

    <!-- 查询帖子详情（包含用户信息） -->
    <select id="selectPostDetail" resultMap="PostDetailResultMap">
        SELECT 
            p.*,
            u.username,
            u.avatar,
            (SELECT COUNT(*) FROM post p2 WHERE p2.user_id = p.user_id) as user_post_count
        FROM post p
        LEFT JOIN user u ON p.user_id = u.user_id
        WHERE p.post_id = #{postId}
    </select>

    <!-- 查询用户发布的帖子统计信息 -->
    <select id="selectUserPostStats" resultType="com.petrescue.petrescuesystem.mapper.PostMapper$PostStats">
        SELECT 
            COUNT(*) as totalPosts,
            COALESCE(SUM(view_count), 0) as totalViews,
            COALESCE(SUM(like_count), 0) as totalLikes,
            COALESCE(SUM(comment_count), 0) as totalComments,
            COALESCE(AVG(view_count), 0) as avgViews
        FROM post 
        WHERE user_id = #{userId}
    </select>

    <!-- 查询指定日期范围内的帖子 -->
    <select id="selectByDateRange" resultMap="BaseResultMap">
        SELECT * FROM post 
        WHERE created_at BETWEEN #{startDate} AND #{endDate}
        ORDER BY created_at DESC
    </select>

    <!-- 统计各状态帖子的数量 -->
    <select id="countByStatus" resultType="com.petrescue.petrescuesystem.mapper.PostMapper$StatusCount">
        SELECT status, COUNT(*) as count 
        FROM post 
        GROUP BY status
        ORDER BY status ASC
    </select>

</mapper>